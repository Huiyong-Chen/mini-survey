import { type Config } from 'prettier';

const config: Config = {
  semi: true,
  singleQuote: true,
  useTabs: false,
  tabWidth: 2,
  printWidth: 100,
  trailingComma: 'all',
};

export default config;
